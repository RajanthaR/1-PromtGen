# Remaining work — PromptForge Studio

**Captured:** 2026-07-14T20:51:04+05:30  
**Branch baseline:** `master` @ Phase 6 merge (`3573ed4` / PR #7)  
**Plan status:** Phases 0–6 implemented and merged. No further numbered phase is defined in `docs/PHASED_PLAN.md`.

This note lists what is still between “code complete for the phased plan” and a credible public launch.

---

## 1. Executive summary

| Area | Status |
| --- | --- |
| Phased engineering (0–6) | Done and merged to `master` |
| Core enhance / refine loop | Implemented |
| Library / history / context | Backend packages + UI largely present |
| Billing / quotas / BYO / privacy | Phase 6 complete (purge **scheduler** not wired) |
| Template **catalog content** | Was placeholder-sized; launch target is **50–100 originals** |
| Production deploy / ops | Not productized in-repo |
| README / operator docs | Stale (still reads like Phase 0) |

---

## 2. Product gaps vs launch spec

### 2.1 Template library (spec §5.3) — priority

**Target:** 50–100 **original** templates across:

content, copywriting, email, SEO, social, research, sales, support, coding, PM, education, data analysis, prompt evaluation.

**Before this remaining-work session:**

- UI hard-coded **~5** templates in `apps/web/src/reuse/reuse-models.ts`
- DB seed had **2** stubs with a **different shape** (string[] variables vs `{ name, label, required }[]`)
- Package tests used **3** short placeholders
- `reuse-api-client` declared `GET /templates` and `POST /templates/:id/fill` but **no API routes** implemented
- Templates page used **client-only** `seedTemplates`, not API/DB

**Done in this session (2026-07-14):**

- [x] Authored **100 original** launch templates across all 13 spec categories
- [x] Single source of truth: `packages/library-templates/content/launch-catalog.json`
- [x] Validated export: `@promptgen/library-templates/catalog` (`launchTemplateCatalog`)
- [x] Web Templates page / `seedTemplates` loads the full catalog
- [x] DB `seedStarterTemplates` reads the same JSON (variable objects fixed)
- [x] Unit tests lock catalog size (100), uniqueness, category coverage

**Still open after catalog content:**

- [ ] HTTP API for list/fill (contracts exist; routes do not)
- [ ] Persist “recently used” per user (UI is session-local today)
- [ ] Align DB `templates.id` (UUID) with stable public slug ids (`tmpl_*`) if API serves from Postgres
- [ ] Idempotent production seed that upserts by public slug (today inserts UUID rows; re-seed may duplicate without a unique slug column)
- [ ] Optional brand/voice editorial pass on the 100 bodies before marketing launch

### 2.2 Reuse layer API completeness

Client contracts in `apps/web/src/reuse/reuse-api-client.ts` include library, history, context, and templates. Confirm each route is implemented on `apps/api` and that UI pages call the client (several pages still look client/demo-seed heavy).

### 2.3 Deferred by explicit product decision (do not build unless scope changes)

From `AGENTS.md` / spec §2 / §12:

- Downstream-task execution (“run this prompt for me”)
- Image / video prompt modes
- MCP server
- Team workspaces / approvals
- Self-hosting packaging
- Autonomous agents / multi-step orchestration
- Dataset optimization / fine-tuning / hosted models
- Vector / semantic search (Postgres FTS is launch default)
- Marketplace
- Second LLM provider (gateway is designed for it; only Gemini wired)

---

## 3. Operations & launch hardening leftovers

| Item | Notes |
| --- | --- |
| Soft-delete **purge scheduler** | Purge **function** implemented & tested in Phase 6; no cron/platform job |
| Production env | Secrets: Gemini, DB, Redis, `BYO_KEY_ENCRYPTION_SECRET`, session/auth |
| Observability | Traces/metrics code present; dashboards/hosting not defined |
| E2E suite | No Playwright; Phase 6 used lint/typecheck/test/eval + smoke |
| Fallback provider | Optional later; not launch-blocking per phased plan |
| Abuse / free tier | Per-user + email verification coded; verify in real auth stack |
| Payment provider | Plans/quotas exist; Stripe (or equivalent) checkout not called out as shipped |

---

## 4. Documentation debt

| Doc | Issue |
| --- | --- |
| `README.md` | Still describes Phase 0 “placeholder apps” |
| Operator runbook | No single “how to run staging/prod” doc |
| Template content ownership | Phase 5 said operator supplies content; catalog now needs review for brand voice |

---

## 5. Suggested order of work (post-catalog)

1. **Ship 100-template catalog** (this session) + validate with `validateTemplateContent`.
2. **Wire Templates UI** to the shared catalog (no divergent hard-codes).
3. **Fix DB seed** to accept full template objects (and optionally slug column).
4. **Implement `/templates` API** and switch the page off pure client seed.
5. **Refresh README** + env checklist for first deploy.
6. **Wire purge job** on the chosen host (Cloudflare cron, k8s CronJob, etc.).
7. **E2E smoke** for enhance → save → template fill → settings export/delete.
8. **Launch checklist**: auth providers, domain, rate limits under load, cost alerts.

---

## 6. Exit criteria for “launch-ready” (informal)

- [ ] ≥50 (target 100) original templates browsable with filters and required-variable fill
- [ ] Enhance/Refine/Improve/Shorten reliable against live Gemini with structured output
- [ ] Free quota + verified email enforced; BYO path works on paid
- [ ] Export + account deletion work; purge scheduled in production
- [ ] WCAG 2.1 AA maintained on primary flows
- [ ] Secrets never logged; training opt-in absent by default
- [ ] Docs match the running system

---

## 7. References

- Spec: `Opus-4.8-plans/improved_web_app_spec.md`
- Plan: `docs/PHASED_PLAN.md`
- Phase 6 security/privacy: `docs/phase-6-security-privacy-audit.md`
- Agent rules: `AGENTS.md`
